import { Balance } from "./Balance.entity";
import { Auction } from "./Auction.entity";

const entities = [
    Auction,
    Balance,
];

export {
    Auction,
    Balance,
};

export default entities;